<?php

// calling:
//   Artisan::call('migrate:refresh'); 
//   Artisan::call('db:seed'); 
// actually runs against the testing database... but the
// specs themselves run against the production database!

// Here you can initialize variables that will for your tests
